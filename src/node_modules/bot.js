var bb = require('bot-brother');
var config = require('config');
var chrono = require('chrono-node');

// Модуль нотификаций
// - добавить уведомление
// - удалить уведомление
// - пушнуть событие, что уведомление готово
// Чтение данных

var bot = bb({
  key: config.bot.key,
  redis: config.redis
});

module.exports = bot;